# The migration builds the normalized workspace reuse schema and separates
# immutable operation facts from mutable operation lease state.
# SQLite foreign keys are temporarily disabled while referenced tables change.
run_in_transaction = false
