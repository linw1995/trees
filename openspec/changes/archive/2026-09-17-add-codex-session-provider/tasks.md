## 1. Provider

- [x] 1.1 Implement the executable script with read-only storage discovery, session filtering, ordering, and name fallback. Verify the executable against local Codex metadata without adding test files.

## 2. Configuration and Local Validation

- [x] 2.1 Document installation and configuration, including offline runtime preparation and exact-root matching. Verify documentation checks pass.
- [x] 2.2 Install the executable and preserve unrelated local Trees configuration. Verify the installed copy matches the repository script.
- [x] 2.3 Build Trees locally and test real workspace JSON and human output. Verify complete lists, first-session display, successful observation, and elapsed time under the configured timeout.
- [x] 2.4 Run local validation and repository checks without adding test files. Record results and validate the OpenSpec change.
